{
  "name": "dawar-saada-maintenance-ticketing-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "npm run build:client && npm run build:server",
    "build:client": "vite build",
    "build:server": "rimraf dist-server && tsc -p tsconfig.server.json && tsc-alias -p tsconfig.server.json",
    "preview": "node dist-server/server.js",
    "start": "node dist-server/server.js"
  },
  "dependencies": {
    "body-parser": "^2.2.2",
    "express": "^5.2.1",
    "react": "^19.2.4",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "rimraf": "^6.1.3",
    "tsc-alias": "^1.8.16",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
